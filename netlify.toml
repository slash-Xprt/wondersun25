[build]
  command = "npm run build"
  publish = "dist"

[dev]
  framework = "#auto"
  targetPort = 3000
  port = 8888
  publish = "dist"
  autoLaunch = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
